apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platyplus-next
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Fluufff/infra-argocd.git
    targetRevision: next
    path: charts/platyplus
    helm:
      releaseName: platyplus-next
      valuesObject:
        host: registration.next.fluufff.org
  destination:
    server: "https://kubernetes.default.svc"
    namespace: platyplus-next
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - RespectIgnoreDifferences=true
  ignoreDifferences:
  - group: apps
    kind: Deployment
    jsonPointers:
    - /spec/replicas
  - kind: Secret
    jqPathExpressions:
    - .data
