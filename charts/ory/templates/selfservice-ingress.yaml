apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: selfservice
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.selfservice.domain }}`)
      services:
        - kind: Service
          name: selfservice
          port: 80
