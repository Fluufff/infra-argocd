apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ory
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Fluufff/infra-argocd.git
    targetRevision: feat/ory
    path: charts/ory
    helm:
      releaseName: ory
  destination:
    server: "https://kubernetes.default.svc"
    namespace: ory
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - kind: Secret
      jqPathExpressions:
        - .data
