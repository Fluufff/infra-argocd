apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: drinkr
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.domain }}`)
      services:
        - kind: Service
          name: drinkr
          port: 80
